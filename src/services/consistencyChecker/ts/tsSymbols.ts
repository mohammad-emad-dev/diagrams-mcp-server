// AST symbol collection as a pure visitor (no filesystem, no matching).
// Phase 1 stub: returns an empty set. Filled in Phase 3.

/** Collect declared symbol names from a parsed source file (stub: empty). */
export function collectTsDeclaredSymbols(_sourceFile: unknown): Set<string> {
  void _sourceFile;
  return new Set<string>();
}
