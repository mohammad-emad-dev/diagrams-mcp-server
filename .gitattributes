# Normalize every text file to LF in the working tree on all platforms so
# checkouts are identical on Windows and Linux runners and the Prettier
# check (default endOfLine "lf") gives the same result everywhere. Git's
# text=auto detection leaves binary files untouched.
* text=auto eol=lf
